{
  "name": "logistic-dos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "to-webp": "node scripts/to-webp.js",
    "build:css": "cleancss -o css/styles.min.css css/styles.css",
    "build:js": "terser js/main.js -o js/main.min.js --compress --mangle",
    "build": "npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "sharp": "^0.34.5",
    "terser": "^5.46.2"
  }
}
